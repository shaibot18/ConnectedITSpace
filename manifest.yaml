---
applications:
- name: CIS.R1.1.2
  host: cisr112
  memory: 1024M
  instances: 1
  env:
    NODE_ENV: production
    NODE_VERBOSE: true
  command: npm start
  buildpack: https://github.com/heroku/heroku-buildpack-nodejs
  path: ./
  services:
  - cis-mongodb