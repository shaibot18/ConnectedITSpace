require('rootpath')();
const UtilService = require('services/util.service');
const http = require('http');
const moment = require('moment');

/**
 * Tool used to test, inject, generate room stat data
 */